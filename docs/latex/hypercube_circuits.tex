\documentclass[11pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{mathpazo}
\usepackage[scaled]{helvet}
\usepackage{microtype}
\usepackage[letterpaper,margin=0.88in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\usepackage{xurl}
\usepackage{hyperref}

\hypersetup{
  pdftitle={Hypercube: Callbacks, Triggers, and Replay},
  pdfauthor={research@strategynet.ai},
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!60!black
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.52em}

\lstdefinestyle{circuit}{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{black!3},
  frame=single,
  rulecolor=\color{black!20},
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  aboveskip=0.8em,
  belowskip=0.8em
}

\newcommand{\SLICE}{\textsf{Slice}}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}

\tikzset{
  circuitbox/.style={
    draw=black!70,
    fill=black!3,
    rounded corners=2pt,
    align=center,
    minimum height=9mm,
    text width=28mm,
    font=\sffamily\fontsize{8}{9.5}\selectfont
  },
  circuithot/.style={
    circuitbox,
    draw=blue!55!black,
    fill=blue!5
  },
  circuitmeta/.style={
    circuitbox,
    draw=black!50,
    fill=black!4
  },
  circuitstate/.style={
    circuitbox,
    draw=blue!65!black,
    fill=blue!8
  },
  circuiteffect/.style={
    circuitbox,
    draw=red!55!black,
    fill=red!4
  },
  circuitflow/.style={
    -{Latex[length=2mm]},
    draw=black!80,
    line width=0.75pt
  },
  circuitrecord/.style={
    -{Latex[length=2mm]},
    draw=blue!55!black,
    dashed,
    line width=0.75pt
  },
  circuitblocked/.style={
    -{Latex[length=2mm]},
    draw=black!55,
    dotted,
    line width=0.75pt
  }
}

\title{Hypercube Circuits: realtime linear algebra with stateful triggers and replay}
\author{research@strategynet.ai}
\date{July 2026}

\begin{document}
\maketitle

\begin{abstract}Hypercube calculates factors and scores for a changing set of 
  instruments. This paper adds callbacks that run after each completed generation. 
  A callback can calculate spreads between factor outputs, wait for a threshold 
  to hold across several updates, use a separate exit threshold, and invalidate 
  a signal when an input disappears. It returns current state and entry, 
  exit, or invalidation events for use by monitors, alerts, and strategy services. 
  A bounded Disruptor ring preserves order and reports \texttt{RingFull} 
  when callbacks cannot keep up. The same run can be written to JSON Lines and 
  replayed through a fresh engine to check the calculations and callback state. 
  The demo replays 40 generations, 6,400 factor cells, 1,280 trigger-state cells, 
  and 78 transitions without a mismatch. Aeron, raw-feed replay, checkpoints, 
  benchmarks, and execution are not yet included.\end{abstract}

\section{Using Hypercube Across Updates}

\subsection{Why the circuit exists}

Hypercube publishes its memory-mapped output as \SLICE{} files.  It already
calculates fields, factors, ranks, basket discrepancies, and residuals for one
complete generation \cite{partone}.  It does not retain application state
between generations.  It therefore cannot remember that a condition has held
for three updates, distinguish a new entry from an active signal, or rebuild
that state from a recording.

Those omissions become visible in an ordinary factor application.  Let
\(f_{i,g}\) and \(h_{i,g}\) be two calculated factors for entity \(i\) at
generation \(g\).  A linear Hypercube node can calculate a spread
\[
  d_{i,g}=f_{i,g}-h_{i,g}.
\]
A trading, alerting, or monitoring application rarely wants a new action on
every update for which \(d_{i,g}>a\).  It usually wants a lifecycle:
\[
  \text{inactive}\rightarrow\text{qualifying}\rightarrow
  \text{active}\rightarrow\text{exited or invalidated}.
\]
That lifecycle requires state across generations.  The circuit runs after
Hypercube completes a generation, retains the qualifying count and active
state for each entity, and reports only genuine state changes.

\subsection{Programs in the repository}

The repository contains four runnable examples \cite{repository}:
\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}P{0.20\textwidth}P{0.32\textwidth}X@{}}
\toprule
Program & Calculation & Output \\
\midrule
browser server &
stock factor graph over a changing cross-section &
\SLICE{} files, a JSON snapshot, and an SSE dashboard \\
ETF monitor &
constituent returns, basket fair values, premiums, and ranks &
ETF-aligned fair value and premium vectors \\
pairs monitor &
cointegrating residuals, standardized scores, half-life, and rank &
pair-aligned residual and opportunity vectors \\
record/replay &
factor calculation plus a persistent threshold callback &
a JSON Lines recording, current trigger state, transitions, and a verification report \\
\bottomrule
\end{tabularx}
\end{center}

The ETF and pairs programs show the calculations.  The browser shows current
output.  The replay program adds the stateful callback and recording used in
this paper.  They run separately but use the same Hypercube update and
snapshot types.

\subsection{Keep state, recordings, and effects separate}

The implementation keeps three outputs separate:
\begin{enumerate}
  \item \textbf{current state}: the latest factor values and callback state;
  \item \textbf{recording}: the ordered updates needed to rebuild that state;
  \item \textbf{effects}: alerts, database writes, or orders sent by a
        separately authorized adapter.
\end{enumerate}

A Disruptor ring only hands work to a callback.  A \SLICE{} file only exposes
the latest value.  Neither is a recording.  Replay recalculates state but must
not resend alerts or orders.

\section{Processing Each Hypercube Update}

\subsection{Process a complete generation}

For generation \(g\), the pure engine receives
\[
  U_g=(g,t_g,m_g,R_g,N_g)
\]
and emits a coherent snapshot
\[
  H_g=E(U_g)=(g,t_g,m_g,n_g,V_g,Q_g).
\]
Here \(R_g\) is the complete entity-row cross-section, \(N_g\) is the declared
node graph, \(V_g\) is the ordered calculated value set, and \(Q_g\) is node
status.  The circuit does not subscribe to individual cell writes.  Its event
is the immutable \(H_g\), after the engine has accepted the complete update.

\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[node distance=7mm and 7mm]
  \node[circuitmeta] (input) {\textbf{Update \(U_g\)}\\rows, graph, logical time};
  \node[circuithot, right=of input] (engine)
    {\textbf{Hypercube}\\calculate full generation};
  \node[circuithot, right=of engine] (snapshot)
    {\textbf{Snapshot \(H_g\)}\\coherent cross-section};
  \node[circuitstate, right=of snapshot] (ring)
    {\textbf{Disruptor}\\bounded ordered handoff};
  \node[circuitstate, right=of ring] (processor)
    {\textbf{Processor}\\declared temporal state};
  \draw[circuitflow] (input) -- (engine);
  \draw[circuitflow] (engine) -- (snapshot);
  \draw[circuitflow] (snapshot) -- (ring);
  \draw[circuitflow] (ring) -- (processor);
  \node[circuitmeta, below=10mm of processor] (outputs)
    {\textbf{Output}\\state vector + transitions};
  \draw[circuitflow] (processor) -- (outputs);
  \node[circuitmeta, below=10mm of input] (record)
    {\textbf{Recording}\\input + expected output};
  \draw[circuitrecord] (input) -- (record);
  \draw[circuitrecord] (outputs) -- (record);
\end{tikzpicture}%
}
\caption{Hypercube finishes a generation before the callback receives it.
The input and callback output are written to the recording.}
\label{fig:circuit-path}
\end{figure}

The per-frame context is
\[
  K_g=(q_g,g,t_g,m_g),
\]
where \(q_g\) is the circuit sequence.  Stateful code uses \(g\) and \(t_g\)
as logical coordinates.  Processor scheduling time, operating-system time,
and measured calculation duration are operational observations, not inputs to
the state transition function.

\subsection{Ordered callback execution with Disruptor}

The LMAX Disruptor separates event storage, sequencing, producer claims,
consumer progress, and dependency graphs around a preallocated bounded ring
\cite{lmax}.  The Rust \texttt{disruptor} crate implements the same family of
patterns for low-latency inter-thread communication and recommends
nonblocking publication when the application must handle a full ring
explicitly \cite{rustdisruptor}.

The current circuit chooses a single producer because Hypercube generations
already have one ordered owner.  One stateful processor consumes them in
sequence.  The ring offers:
\begin{itemize}
  \item a preallocated bounded handoff rather than unbounded queue growth;
  \item a circuit sequence independent of wall-clock timestamps;
  \item nonblocking submission with a visible \texttt{RingFull} result;
  \item configurable sleep, spin-loop, and busy-spin waiting;
  \item an asynchronous submit/receive API and a lockstep correctness path.
\end{itemize}

It does \emph{not} make an overloaded consumer current.  If producer sequence
\(p\), consumer frontier \(c\), and ring capacity \(B\) satisfy
\[
  p-c\ge B,
\]
the next nonblocking publication is rejected.  That failure is valuable
information: the consumer cannot keep up and latency would otherwise grow
behind the producer.

\subsection{When the callback falls behind}

The circuit never silently overwrites an unprocessed correctness-lane
generation.  On a full ring, an owner must choose one of three policies:
\begin{enumerate}
  \item backpressure the producer and preserve every accepted generation;
  \item retain normalized input durably and catch up from its log position;
  \item coalesce or drop only a separately named best-effort view.
\end{enumerate}

\texttt{CaptureSession} and \texttt{ReplayRunner} use lockstep processing.
The engine does not accept the next update until callback processing and the
recording step complete.  Where an upstream market feed cannot be slowed, the
feed should be archived before generation assembly.  The circuit is then a
deterministic consumer of durable input, not the sole owner of history.

The ring does not provide raw-feed storage, cross-host recovery, multi-stream
generation assembly, or duplicate protection for external effects.

\subsection{Requirements for repeatable callbacks}

A processor is a state transition function
\[
  (S_g,O_g)=P(S_{g-1},K_g,H_g),
\]
where \(S_g\) is private declared state and \(O_g\) is the published
deterministic output.  Replay is meaningful when:
\begin{enumerate}
  \item \(P\) reads only \(S_{g-1}\), \(K_g\), \(H_g\), and immutable
        configuration;
  \item iteration and output ordering are canonical;
  \item missing data and reset behavior are specified;
  \item nondeterministic I/O, random sources, and wall time stay outside \(P\);
  \item irreversible effects consume \(O_g\) through another adapter.
\end{enumerate}

Logical ordering has a longer history than this implementation.  Lamport
showed that event ordering can be represented independently of synchronized
physical clocks \cite{lamport}.  Hypercube does not implement a distributed
Lamport clock, but it applies the relevant separation: generation and circuit
sequence determine processing order, while observation time describes the
input.

\subsection{Persistent threshold callback}

The first processor watches one calculated node for every entity.  A trigger
specification is
\[
  T=(\mathrm{id},\mathrm{node},a,b,p),
\]
with entry threshold \(a\), exit threshold \(b\), required persistence \(p\),
and strict hysteresis \(a>b\).

For an inactive entity with observed value \(x_{i,g}\), the qualifying count is
\[
  k_{i,g}=
  \begin{cases}
    k_{i,g-1}+1,&x_{i,g}\ge a,\\
    0,&x_{i,g}<a.
  \end{cases}
\]
The state becomes active when \(k_{i,g}\ge p\).  An active state remains active
through the hysteresis band and exits only when
\[
  x_{i,g}\le b.
\]
If the watched node has no value for an active entity, the state is
\emph{invalidated}, not silently exited.  If a qualifying but inactive entity
is missing, its count is removed, so a later value cannot falsely complete a
``consecutive'' run across a gap.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=13mm and 18mm]
  \node[circuitmeta] (inactive) {\textbf{Inactive}\\\(k=0\)};
  \node[circuitstate, right=of inactive] (qualifying)
    {\textbf{Qualifying}\\\(0<k<p\)};
  \node[circuithot, right=of qualifying] (active)
    {\textbf{Active}\\entry emitted};
  \draw[circuitflow] (inactive) --
    node[above,font=\scriptsize\sffamily]{\(x\ge a\)} (qualifying);
  \draw[circuitflow] (qualifying) --
    node[above,font=\scriptsize\sffamily]{\(k\ge p\)} (active);
  \draw[circuitflow] (qualifying) to[bend left=33]
    node[below,font=\scriptsize\sffamily]{\(x<a\) or missing} (inactive);
  \draw[circuitflow] (active) to[bend left=32]
    node[above,font=\scriptsize\sffamily]{\(x\le b\): exit} (inactive);
  \draw[circuitblocked] (active) to[loop above]
    node[above,font=\scriptsize\sffamily]{\(x>b\)} (active);
  \node[circuitmeta, below=15mm of active] (invalid)
    {\textbf{Absent}\\invalidation emitted};
  \draw[circuitflow] (active) --
    node[right,font=\scriptsize\sffamily]{missing} (invalid);
\end{tikzpicture}
\caption{Persistent upper-threshold trigger with hysteresis and explicit
missing-data invalidation.}
\label{fig:trigger-state}
\end{figure}

\subsection{Publish current state and transitions}

For every generation, the processor emits:
\[
  Z_g=\{(\mathrm{trigger},\mathrm{node},i,
          \mathrm{active}_{i,g},k_{i,g})\}
\]
and
\[
  \Delta Z_g=\{\mathrm{Entered},\mathrm{Exited},
                \mathrm{Invalidated}\text{ events at }g\}.
\]

\(Z_g\) is the complete ordered trigger-state cross-section.  It maps
naturally to a new \SLICE{} vector and lets a late subscriber bootstrap without
replaying the entire run.  \(\Delta Z_g\) is the sparse edge stream for a
subscriber that already holds \(Z_{g-1}\).  Recording both views also exposes
latent state divergence even when no transition happens in a generation.

An application can write \(Z_g\) to a \SLICE{} vector so a new subscriber can
read current state immediately.  Existing subscribers can consume
\(\Delta Z_g\) to receive only entries, exits, and invalidations.  Neither
consumer needs to rerun the factor calculation or the threshold callback.

\section{Recording and Replay}

\subsection{What to record}

``Replay the system'' is incomplete until the recorded boundary is named.
\begin{center}
\begin{tabularx}{\textwidth}{@{}p{0.20\textwidth}p{0.27\textwidth}X@{}}
\toprule
Recorded boundary & What replay reconstructs & Tradeoff \\
\midrule
published factor frame &
callbacks, strategies, and downstream state only &
smallest record; cannot verify factor calculation \\
complete Hypercube update &
factor graph, values, callback state, and transitions &
implemented compromise; repeats rows and graph per generation \\
normalized raw events plus generation seals &
ingestion, generation assembly, factors, and callbacks &
rebuilds the most; requires ordering, watermark, and join rules \\
\bottomrule
\end{tabularx}
\end{center}

The current implementation records complete Hypercube updates.  It is more
useful than factor-only replay because changes to rows, node graphs, transforms,
or arithmetic can be detected.  It stops short of claiming that vendor
normalization and multi-feed generation assembly are reproducible.

\subsection{Recording format}

A recording is
\[
  \mathcal D=(M,G_0,G_1,\ldots,G_n).
\]
The manifest
\[
  M=(v,\mathrm{run},\mathrm{build},\mathrm{config},
     \mathrm{layout},\mathrm{triggers},\mathrm{metadata})
\]
identifies the schema and non-generation configuration.  One generation
record is
\[
  G_g=(q_g,U_g,F_g,D_g,Z_g,\Delta Z_g),
\]
where \(F_g\) is an optional map from stable upstream stream identity to the
last position included in the generation and \(D_g\) is the expected semantic
snapshot digest.

The development adapter writes one manifest and then one generation per JSON
line.  It validates:
\begin{itemize}
  \item exactly one leading version-1 manifest;
  \item contiguous circuit sequences and increasing generations;
  \item agreement between update and expected output generation;
  \item valid trigger configuration and nonempty provenance fields;
  \item only finite JSON numeric inputs.
\end{itemize}

JSON parsing enables exact finite \(f64\) round trips.  NaN and infinity are
rejected before the engine advances because JSON would otherwise encode them
as \texttt{null}; a missing primitive is represented by omitting its field.
The format is intentionally inspectable and not presented as the final
low-latency wire encoding.

\subsection{What replay compares}

The semantic snapshot digest includes:
\begin{itemize}
  \item generation, observation time, and entity count;
  \item ordered node and entity identities;
  \item exact IEEE-754 value bits and value observation times;
  \item ordered status identities and value/missing counts.
\end{itemize}

It excludes:
\begin{itemize}
  \item \texttt{ExecutionMode}, because \texttt{Live} becomes
        \texttt{Replay};
  \item measured node compute duration;
  \item wall time, thread identity, transport delay, and logging details.
\end{itemize}

The current digest is a versioned streaming xxHash64 over a canonical byte
representation.  Digest equality is supplemented by exact comparison of the
full ordered state and transition outputs.  The build and configuration
identities remain provenance: a different compiler, target, or implementation
may be replayed, but any arithmetic difference is reported rather than
normalized away.

Replay compares calculated values and state transitions.  It does not compare
processor scheduling, compute duration, or transport timing.

\subsection{Replay procedure}

Replay performs the following bounded procedure:
\begin{enumerate}
  \item validate and read the recording manifest;
  \item construct a new \texttt{HypercubeEngine};
  \item construct a new processor from the recorded trigger configuration;
  \item set each recorded update mode to \texttt{Replay};
  \item calculate the snapshot and process it in lockstep;
  \item compare circuit sequence, semantic digest, complete state, and
        transitions;
  \item count every divergent generation and retain the first concrete
        expected-versus-actual difference.
\end{enumerate}

Formally,
\[
  H'_g=E'(U_g[m\leftarrow\mathrm{Replay}]),
\]
\[
  (S'_g,O'_g)=P'(S'_{g-1},K'_g,H'_g).
\]
An exact replay requires
\[
  q'_g=q_g,\qquad D(H'_g)=D_g,\qquad
  Z'_g=Z_g,\qquad\Delta Z'_g=\Delta Z_g
\]
for every recorded generation.

Research on deterministic component replay likewise separates virtual or
logical ordering from incidental runtime scheduling \cite{detreplay}.  The
Hypercube implementation is narrower: it records already coherent
application updates and executes one ordered stateful lane.

\subsection{Mismatch reporting and exit codes}

Malformed files, invalid updates, and processor failures are operational
errors.  A valid recording that produces a different digest, state, or
transition is a semantic result.  The CLI therefore uses separate exits:
\begin{center}
\begin{tabular}{@{}cl@{}}
\toprule
Exit & Meaning \\
\midrule
0 & every recorded generation matched \\
1 & operational or recording-format failure \\
2 & replay completed with semantic divergence \\
\bottomrule
\end{tabular}
\end{center}

This distinction supports two workflows:
\begin{itemize}
  \item \textbf{reconstruction}: expect zero divergence under the same
        implementation contract;
  \item \textbf{counterfactual replay}: intentionally change a node, threshold,
        or model and analyze the resulting divergence.
\end{itemize}

\subsection{Disable external effects during replay}

Replay data must not reach an adapter that can send an order, notification,
or database write as though the data were live.  The current implementation
contains no execution adapter, and callbacks perform calculations only.

Future replay publication should use a namespace such as
\[
  \texttt{replay/\{run\_id\}/\ldots}
\]
and every effect adapter must reject replay-mode inputs.  A live order,
notification, or database mutation should also carry an idempotency key and a
reference to the generation that produced it.

\section{Deployment}

\subsection{Use Aeron at a process boundary}

Inside one compute process, Aeron is unnecessary for the engine-to-callback
handoff.  The immutable snapshot is already in memory; the Disruptor provides
ordered inter-thread transfer without serialization or a media driver.

Aeron becomes useful at a process or host boundary:
\begin{itemize}
  \item factor/state publication to independent services;
  \item durable capture of normalized inputs or generation envelopes;
  \item subscriber recovery from a known stream position;
  \item controlled replay onto a dedicated channel.
\end{itemize}

Aeron Archive records and replays streams from durable storage and supports
replay from a specified recording and position \cite{aeron}.  The current
crate does not depend on Aeron.  Its public manifest and generation structs are
transport-neutral envelopes that a future SBE adapter can encode as:
\begin{lstlisting}[style=circuit]
hypercube.recording.manifest.v1
hypercube.recording.generation.v1
\end{lstlisting}

\subsection{Record upstream stream positions}

For \(k\) upstream streams, generation \(g\) may record
\[
  F_g=(p_{1,g},p_{2,g},\ldots,p_{k,g}),
\]
where \(p_{j,g}\) is the last input position included from stream \(j\).
The manifest can carry Archive recording identities and start positions;
\texttt{source\_positions} carries the per-generation frontier.

These positions show which upstream messages were included in a generation.
The feed adapter must still define:
\begin{itemize}
  \item the canonical order when inputs from different streams race;
  \item event-time and ingestion-time watermark rules;
  \item late, duplicate, corrected, and missing event behavior;
  \item the generation seal that says no more input belongs to \(g\).
\end{itemize}

Without those rules, Hypercube can replay complete updates but cannot rebuild
them from raw feeds.

\subsection{Deployment options}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[node distance=7mm and 9mm]
  \node[circuitmeta] (feed) {\textbf{Normalized feed}\\Aeron or local adapter};
  \node[circuitmeta, right=of feed] (assembler)
    {\textbf{Assembler}\\coherent update + frontiers};
  \node[circuithot, right=of assembler] (cube)
    {\textbf{Hypercube}\\factors and scores};
  \node[circuitstate, right=of cube] (circuit)
    {\textbf{Circuit}\\state and transitions};
  \node[circuitmeta, right=of circuit] (views)
    {\textbf{Views}\\Slice, Aeron, API};
  \draw[circuitflow] (feed) -- (assembler);
  \draw[circuitflow] (assembler) -- (cube);
  \draw[circuitflow] (cube) -- (circuit);
  \draw[circuitflow] (circuit) -- (views);
  \node[circuitmeta, below=10mm of assembler] (archive)
    {\textbf{Archive}\\raw or update boundary};
  \draw[circuitrecord] (feed) -- (archive);
  \draw[circuitrecord] (assembler) -- (archive);
  \node[circuiteffect, below=10mm of views] (effect)
    {\textbf{Effect adapter}\\separate authority};
  \draw[circuitblocked] (views) -- (effect);
\end{tikzpicture}%
}
\caption{Target deployment: live calculation (solid), recording (dashed), and
separately authorized effects (dotted).}
\label{fig:deployment}
\end{figure}

All components can run in one process.  A split service can keep Hypercube and
the callback together while publishing state over Aeron.  A deployment that
must rebuild from raw input can archive normalized messages before generation
assembly and publish replay on a separate channel.

\section{Examples}

\subsection{ETF basket premiums}

The ETF monitor reduces 160 correlated constituent returns into 12 basket fair
values and then evaluates five ETF-aligned Hypercube nodes.  For ETF \(j\),
\[
  r^{\mathrm{NAV}}_{j,g}=\sum_i w_{ji}r_{i,g},\qquad
  V_{j,g}=V_{j,g-1}(1+r^{\mathrm{NAV}}_{j,g}),
\]
while the simulated market premium follows
\[
  u_{j,g}=0.88u_{j,g-1}+0.00040\eta_{j,g}.
\]
The graph publishes fair value, market price, premium basis points, model
z-score, and cross-sectional premium rank.  The terminal therefore displays
60 cells per generation.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\linewidth]{assets/etf-demo.png}
\caption{Checked-in ETF recording at generation 28.  Labels preserve the
premium sign independently of color.  Fees, borrow, and creation-unit
frictions remain explicit omissions.}
\label{fig:etf-demo}
\end{figure}

A callback can watch \texttt{premium\_z}, require it to remain above a
threshold for several generations, and publish an entry or exit without
changing the basket calculation.  A trading component would still need
inventory, quoted spread, creation-unit size, costs, and execution limits.

\subsection{Pairs residuals and triggers}

The pairs monitor publishes 120 cells over 24 stable pair entities.  Its
residual and standardization are
\[
  y_{j,g}=\log A_{j,g}-\alpha_j-\beta_j\log B_{j,g},
\qquad
  z_{j,g}=\frac{y_{j,g}}{\sigma_j/\sqrt{1-\phi_j^2}}.
\]
Half-life and cross-sectional opportunity rank remain separate nodes.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\linewidth]{assets/pairs-demo.png}
\caption{Checked-in pairs recording at generation 28.  The display ranks
standardized dislocations while retaining hedge ratio and half-life context.}
\label{fig:pairs-demo}
\end{figure}

The threshold callback can enter only after the \(z\)-score remains large for
several generations and exit at a lower threshold.  If either leg disappears,
it reports invalidation rather than convergence.  Estimation windows,
structural-break detection, borrow, costs, and portfolio limits remain outside
the demo.

\subsection{Browser and \SLICE{} output}

The synthetic server computes five nodes for a changing stock cross-section,
publishes them to entity-aligned \SLICE{} files, exposes a JSON snapshot, and
streams generations through SSE to a dependency-free dashboard.

The callback adds two outputs:
\begin{itemize}
  \item a dense current-state projection suitable for \SLICE{} or a bootstrap
        response;
  \item a sparse transition stream suitable for already synchronized
        subscribers.
\end{itemize}
The browser stream and \SLICE{} files contain current output, not replay
history.  The JSON Lines recording contains the updates needed for replay.

\subsection{Replay demo}

The executable experiment is:
\begin{lstlisting}[style=circuit]
cargo run -p hypercube-circuit --bin hypercube-replay -- \
  record-demo /tmp/hypercube-factor.jsonl 40 32

cargo run -p hypercube-circuit --bin hypercube-replay -- \
  verify /tmp/hypercube-factor.jsonl
\end{lstlisting}

The synthetic graph has five nodes over 32 entities for 40 generations:
\[
  5\cdot32\cdot40=6{,}400
\]
factor cells.  One threshold specification yields
\[
  32\cdot40=1{,}280
\]
complete trigger-state cells.

\begin{center}
\begin{tabularx}{0.82\textwidth}{@{}X r r@{}}
\toprule
Compared object & Recorded & Replayed \\
\midrule
generations & 40 & 40 \\
factor cells represented by semantic digests & 6,400 & 6,400 \\
trigger-state cells & 1,280 & 1,280 \\
transitions & 78 & 78 \\
divergent generations & \multicolumn{2}{r}{0} \\
\bottomrule
\end{tabularx}
\end{center}

The file contains 41 lines: one manifest and 40 generation records.  This is a
functional exactness result, not a throughput or storage benchmark.
The repository also includes an animated
\href{https://github.com/ingvor-restrat/hypercube/blob/main/docs/markup/circuit-replay.gif}
{Hypercube terminal demo} that shows qualifying state, entries, exits,
missing-data invalidation, and the final replay comparison.

\subsection{Failure cases covered by tests}

The circuit tests do more than assert the happy path:
\begin{itemize}
  \item a blocked consumer fills a two-slot ring and the third generation
        returns \texttt{RingFull} without overwriting pending work;
  \item a missing inactive candidate resets persistence, while a missing active
        entity emits \texttt{Invalidated};
  \item sequence gaps and non-increasing generations are rejected;
  \item non-finite JSON input is rejected before a generation is written;
  \item live and replay JSON values reproduce exact floating-point bits;
  \item a changed recorded primitive produces a snapshot-digest divergence;
  \item unchanged input reconstructs snapshot, state, and transitions.
\end{itemize}

The tests cover overload, sequence gaps, missing observations, serialization,
state reset, and output comparison.

\subsection{Test results}

The workspace currently runs 29 tests:
\begin{center}
\begin{tabularx}{0.86\textwidth}{@{}X r l@{}}
\toprule
Suite & Tests & Result \\
\midrule
engine, publisher, and synthetic injectors & 6 & passed \\
ETF and pairs example invariants & 2 & passed \\
\SLICE{} layout, mmap, records, and vector algebra & 7 & passed \\
circuit, triggers, recording, digest, and replay & 14 & passed \\
\midrule
\textbf{Total} & \textbf{29} & \textbf{passed} \\
\bottomrule
\end{tabularx}
\end{center}

The workspace also passes formatting, clippy with warnings denied, and public
API documentation with warnings denied.  These are conformance and packaging
checks; the repository still makes no latency or throughput claim.

\section{Practical Use and Next Steps}

\subsection{Factor-to-signal service}

A live service can run the components in this order:
\begin{enumerate}
  \item a feed adapter assembles one complete Hypercube update;
  \item Hypercube calculates fields, spreads, ranks, residuals, and scores;
  \item the circuit runs its callbacks on the completed snapshot;
  \item the caller writes current callback state to \SLICE{} or an API and
        sends transitions to alerting or strategy consumers;
  \item the capture session records the update and callback output for replay.
\end{enumerate}

For example, suppose an ETF premium \(z\)-score must be at least \(1.5\) for
three consecutive generations and exits at \(0.5\).  Hypercube calculates the
\(z\)-score.  The callback increments a per-ETF count, emits
\texttt{Entered} on the third qualifying generation, keeps the state active
while the value remains above \(0.5\), and emits \texttt{Invalidated} if the
ETF value disappears.  A new consumer reads the complete current state;
an existing consumer can process only the transition events.  The recording
lets an operator rerun the same generations when the factor code or trigger
code changes.

\subsection{Test rule changes against recorded data}

After an exact replay passes, another run can intentionally:
\begin{itemize}
  \item change entry, exit, or persistence parameters;
  \item replace one factor node or model version;
  \item compare old and new state transitions by generation;
  \item inject delayed or missing data according to a declared scenario;
  \item measure proposed decision intents without enabling live effects.
\end{itemize}

The current CLI verifies only the configuration stored in the recording.  A
counterfactual command would accept a different graph or trigger
configuration, mark the run as non-exact, and report the first generation
whose values or transitions changed.

\subsection{Current limits}

The current code covers one complete calculation-and-replay path:
\begin{itemize}
  \item JSON Lines is the only recording adapter; Aeron Archive and SBE mapping
        are specified but not implemented;
  \item complete updates are replayed, not raw vendor events or generation
        assembly;
  \item one threshold processor is provided; general callback composition,
        window state, and model artifacts need additional schemas;
  \item replay begins at generation zero; state checkpoints and seek are not
        implemented;
  \item xxHash64 detects ordinary divergence but is not a cryptographic
        authenticity proof;
  \item state vectors are public structs but no dedicated trigger-to-\SLICE{}
        publisher is included;
  \item no benchmark justifies a wait strategy, ring size, or deployment
        topology for production;
  \item no external-effect or trading authority exists in the circuit crate.
\end{itemize}

The \texttt{verify} command is therefore a calculation check, not a production
recovery tool.  Trigger state is data, not an executable order.

\subsection{Next steps}

The next implementation work is:
\begin{enumerate}
  \item publish trigger state to \SLICE{} and transitions onto a named
        subscriber channel;
  \item add an SBE codec and record the envelopes through Aeron Archive;
  \item define multi-stream generation seals and late-event handling;
  \item add processor checkpoints and verified seek;
  \item add counterfactual configuration and first-mismatch comparison;
  \item benchmark latency, backlog, CPU, allocation, and recovery time for each
        supported deployment;
  \item add an idempotent simulated effect adapter before considering live
        execution.
\end{enumerate}

\subsection{Conclusion}

Hypercube can now calculate a complete generation, pass it to an ordered
stateful callback, return current state and transitions, record the input and
output, and verify the result with a fresh process.  The included threshold
callback handles persistence, separate entry and exit levels, and missing
inputs.

The immediate use is a factor-to-signal service for monitors, alerts, and
strategy inputs.  ETF and pairs provide calculation examples, the browser
provides current output, and the replay demo checks 40 recorded generations.
Publishing callback state to \SLICE{} is the next practical addition.  Aeron
transport, raw-feed reconstruction, checkpoints, benchmarks, and execution
remain future work.

\begin{thebibliography}{9}

\bibitem{partone}
StrategyNet,
\emph{Hypercube: Current analytical state without rebuilding it
for every reader},
Open-source working paper, version 0.2, July 2026,
\url{https://github.com/ingvor-restrat/hypercube/blob/main/docs/latex/hypercube_foundations.pdf}.

\bibitem{lmax}
M. Thompson, D. Farley, M. Barker, P. Gee, and A. Stewart,
\emph{LMAX Disruptor: High performance alternative to bounded queues for
exchanging data between concurrent threads},
LMAX Exchange, May 2011,
\url{https://lmax-exchange.github.io/disruptor/disruptor.html}.

\bibitem{rustdisruptor}
\emph{disruptor 4.3.0: Low latency inter-thread communication via a ring
buffer},
Rust crate documentation,
\url{https://docs.rs/disruptor/4.3.0/disruptor/}.

\bibitem{lamport}
L. Lamport,
\emph{Time, Clocks, and the Ordering of Events in a Distributed System},
Communications of the ACM, 21(7), pp.~558--565, 1978,
\url{https://www.microsoft.com/en-us/research/publication/time-clocks-ordering-events-distributed-system/}.

\bibitem{detreplay}
R. Strom, C. Dorai, T. H. Feng, and Z. Wei,
\emph{Deterministic Replay for Transparent Recovery in Component-Oriented
Middleware},
IEEE ICDCS, 2009,
\url{https://doi.org/10.1109/ICDCS.2009.79}.

\bibitem{aeron}
Aeron Project,
\emph{Aeron Archive Overview},
\url{https://aeron.io/docs/aeron-archive/overview/}.

\bibitem{repository}
StrategyNet,
\emph{Hypercube public repository},
\url{https://github.com/ingvor-restrat/hypercube}.

\end{thebibliography}

\end{document}
