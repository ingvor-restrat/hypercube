[package]
name = "hypercube-circuit"
description = "Stateful generation processing and deterministic record/replay for Hypercube"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["analytics", "replay", "disruptor", "event-processing"]
categories = ["data-structures", "concurrency"]

[dependencies]
disruptor.workspace = true
hypercube-engine = { path = "../hypercube", version = "0.1.0" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
xxhash-rust = { version = "0.8", features = ["xxh64"] }
